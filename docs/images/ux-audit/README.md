# UX audit assets

Matched before/after screenshots of the dashboard, referenced from the
visual-hierarchy issue. Captured at 1400px through a local proxy that injects
the proposal over the shipped build on a real `screp.db`; each pair holds the
same replay, selected row and active filters, so the only difference between
frames is the proposal.

Do not edit these in place — regenerate them if the proposal changes.
